export component Cell inherits Rectangle {
    in property <string> marker: "";

    callback clicked;

    Rectangle {
        background: #dcdcdc;
        x: 0px;
        y: 0px;

        Text {
            text: marker;
            font-size: 60px;
            font-weight: 700;
            color: black;
        }

        touch := TouchArea {
            clicked => {
                root.clicked();
            }
            width: 100%;
            height: 100%;
        }
    }
}